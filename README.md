# Develop

Install dependencies using `yarn` (or `npm i`).

Start application:
```bash
yarn start
```

# Deploy

```bash
yarn run deploy
```

# Show logs of deployed app

```bash
yarn add -g now-logs
now-logs frontendconf
```

